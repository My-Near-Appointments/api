version: '3'
services:
  backend:
    image: ./
    ports:
      - "3000:3000"
    depends_on:
      - db
  db:
    image: postgres
    restart: always
    volumes:
      - /data/postgres:/data/postgres